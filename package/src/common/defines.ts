export declare type ImageElement =
    | HTMLCanvasElement
    | ImageBitmap
    | OffscreenCanvas
    | HTMLImageElement;

export interface Rect {
    left: number;
    right: number;
    top: number;
    bottom: number;
}

export interface Point {
    x: number;
    y: number;
}
